{
  "name": "route-toute",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "vercel-build": "bash info.sh"
  },
  "devDependencies": {
    "turbo": "2.10.2"
  },
  "packageManager": "pnpm@10.33.0"
}
